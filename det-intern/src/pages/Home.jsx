import React from 'react';
import Hero from '../components/hero/Hero';

/**
 * Home Page
 * Renders the Hero section (Commit 3).
 * Future homepage sections will be appended below in upcoming commits.
 */
const Home = () => {
  return (
    <div className="flex-grow">
      <Hero />
    </div>
  );
};

export default Home;
